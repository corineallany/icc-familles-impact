/* Fenêtre de présence : H-20 jusqu'à Hfin-15, puis correction a posteriori. */
function lateMinutes(m,time){if(!m?.planned_start||!time)return 0;const s=new Date(m.planned_start),[h,mi]=time.split(':').map(Number),a=new Date(s);a.setHours(h,mi,0,0);return Math.max(0,Math.round((a-s)/60000))}
function meetingActionKeyV8(m,kind){const f=m&&db.families.find(x=>+x.id===+m.family_id),type=f?.family_type==='FIJ'?'fij':'fi';return kind==='attendance'?`attendance.${type}.manage`:kind==='reporting'?`reporting.${type}.create`:`meetings.${type}.manage`}
function canMeetingActionV8(m,kind){return !!m&&(typeof canActionV6==='function'?canActionV6(meetingActionKeyV8(m,kind),m.family_id):isDirection())}
const _openAttendanceRules=openAttendance;openAttendance=function(kind,id){if(kind!=='meeting')return _openAttendanceRules(kind,id);const m=db.meetings.find(x=>+x.id===id);if(!m)return;if(!canMeetingActionV8(m,'attendance'))return alert('Vous n’avez pas la permission de gérer les présences de cette rencontre.');const phase=meetingPhase(m),live=attendanceWindow(m);if(phase==='upcoming'&&!live)return alert('Les présences ouvrent 20 minutes avant le début de la rencontre.');const members=eligibleMembers(kind,id),rows=members.map(mem=>{const a=db.attendance.find(x=>x.member_id===mem.id&&x.meeting_id===id),shown=!!a?.present;return `<div class="attendance-row attendance-compact"><label class="attendance-person"><input type="checkbox" class="att-present" data-member="${mem.id}" ${shown?'checked':''} onchange="toggleAttendanceFieldsV12(${mem.id},${id},this.checked)"> <span><b>${esc(memberName(mem.id))}</b><small>${esc(familyName(memberFamilyId(mem.id)))}</small></span></label><div class="attendance-detail" data-member="${mem.id}" style="display:${shown?'grid':'none'}"><label class="att-time-label"><span>Arrivée</span><input type="time" class="att-time" data-member="${mem.id}" value="${a?.arrival_time?.slice(0,5)||''}" ${a?.arrival_unknown?'disabled':''} onchange="syncLateReason(${mem.id},${id})"></label><label class="unknown"><input type="checkbox" class="att-unknown" data-member="${mem.id}" ${a?.arrival_unknown?'checked':''} onchange="toggleUnknown(this);syncLateReason(${mem.id},${id})"> Heure non connue</label><input class="att-late-reason" data-member="${mem.id}" placeholder="Motif du retard (facultatif)" value="${esc(a?.late_reason||'')}" style="display:${a?.is_late?'block':'none'}"></div></div>`}).join('');modal.innerHTML=`<div class="modal-card wide"><button class="close" onclick="closeModal()">×</button><h2>Présences — ${esc(m.title)}</h2><p class="muted">${live?'Saisie en temps réel. Touchez une personne lorsqu’elle arrive : l’heure apparaît alors.':'Rencontre passée : vous pouvez compléter ou corriger les présences.'}</p><div class="attendance-list">${rows||'<p class="muted">Aucun membre dans votre périmètre.</p>'}</div><button class="primary" onclick="saveMeetingAttendance(${id},${live})">Enregistrer les présences</button></div>`;modal.classList.remove('hidden')}
function toggleAttendanceFieldsV12(mid,id,checked){const box=document.querySelector(`.attendance-detail[data-member="${mid}"]`);if(!box)return;box.style.display=checked?'grid':'none';if(checked){const t=box.querySelector('.att-time');if(t&&!t.value)t.value=new Date().toTimeString().slice(0,5);syncLateReason(mid,id)}}
const _openMeetingRules=openMeeting;openMeeting=function(id){const m=db.meetings.find(x=>x.id===id);if(!m)return;const phase=meetingPhase(m),live=attendanceWindow(m),past=phase==='past',canAtt=canMeetingActionV8(m,'attendance'),canRep=canMeetingActionV8(m,'reporting'),canManage=canMeetingActionV8(m,'manage');modal.innerHTML=`<div class="modal-card"><button class="close" onclick="closeModal()">×</button><h2>${esc(m.title)}</h2><p>${m.scheduled_date} · ${familyName(m.family_id)}</p><div class="row-actions">${canAtt&&(live||past)?`<button class="primary" onclick="openAttendance('meeting',${id})">✓ ${live?'Présences en cours':'Présences a posteriori'}</button>`:''}${canRep&&past&&m.reporting_required!==false?`<button class="primary" onclick="openReporting(${id})">Reporting</button>`:''}${canManage?`<button class="ghost" onclick="scheduleChange('meeting',${id},'postponed')">Reporter</button><button class="ghost" onclick="scheduleChange('meeting',${id},'cancelled')">Annuler</button>`:''}</div>${!live&&!past&&canAtt?'<p class="muted">Les présences ouvriront 20 minutes avant le début.</p>':''}</div>`;modal.classList.remove('hidden')};