/* Fenêtre de présence : H-20 jusqu'à Hfin-15, puis correction a posteriori. */
function lateMinutes(m,time){if(!m?.planned_start||!time)return 0;const s=new Date(m.planned_start),[h,mi]=time.split(':').map(Number),a=new Date(s);a.setHours(h,mi,0,0);return Math.max(0,Math.round((a-s)/60000))}
const _openAttendanceRules=openAttendance;openAttendance=function(kind,id){if(kind!=='meeting')return _openAttendanceRules(kind,id);const m=db.meetings.find(x=>x.id===id);if(!m)return;const phase=meetingPhase(m),live=attendanceWindow(m);if(phase==='upcoming'&&!live)return alert('Les présences ouvrent 20 minutes avant le début de la rencontre.');const members=eligibleMembers(kind,id),rows=members.map(mem=>{const a=db.attendance.find(x=>x.member_id===mem.id&&x.meeting_id===id);return `<div class="attendance-row attendance-rich"><label><input type="checkbox" class="att-present" data-member="${mem.id}" ${a?.present?'checked':''}> <b>${esc(memberName(mem.id))}</b><small>${esc(familyName(memberFamilyId(mem.id)))}</small></label><input type="time" class="att-time" data-member="${mem.id}" value="${a?.arrival_time?.slice(0,5)||''}" ${a?.arrival_unknown?'disabled':''} onchange="syncLateReason(${mem.id},${id})"><label class="unknown"><input type="checkbox" class="att-unknown" data-member="${mem.id}" ${a?.arrival_unknown?'checked':''} onchange="toggleUnknown(this);syncLateReason(${mem.id},${id})"> Heure non connue</label><input class="att-late-reason" data-member="${mem.id}" placeholder="Motif du retard" value="${esc(a?.late_reason||'')}" style="display:${a?.is_late?'block':'none'}"></div>`}).join('');modal.innerHTML=`<div class="modal-card wide"><button class="close" onclick="closeModal()">×</button><h2>Présences — ${esc(m.title)}</h2><p class="muted">${live?'Saisie en temps réel.':'Rencontre terminée : saisie/correction a posteriori.'} L’heure d’arrivée permet de calculer automatiquement le retard.</p><div class="attendance-list">${rows||'<p class="muted">Aucun membre dans votre périmètre.</p>'}</div><button class="primary" onclick="saveMeetingAttendance(${id},${live})">Enregistrer les présences</button></div>`;modal.classList.remove('hidden')}
function syncLateReason(mid,id){const m=db.meetings.find(x=>x.id===id),t=document.querySelector(`.att-time[data-member="${mid}"]`),u=document.querySelector(`.att-unknown[data-member="${mid}"]`),r=document.querySelector(`.att-late-reason[data-member="${mid}"]`);if(!r)return;r.style.display=(!u?.checked&&lateMinutes(m,t?.value)>0)?'block':'none'}
async function saveMeetingAttendance(id,live){const m=db.meetings.find(x=>x.id===id),rows=[...document.querySelectorAll('.att-present')];for(const c of rows){const member_id=+c.dataset.member,t=document.querySelector(`.att-time[data-member="${member_id}"]`),u=document.querySelector(`.att-unknown[data-member="${member_id}"]`),lr=document.querySelector(`.att-late-reason[data-member="${member_id}"]`),mins=!u.checked?lateMinutes(m,t.value):0;const payload={meeting_id:id,member_id,family_id:memberFamilyId(member_id),present:c.checked,arrival_time:c.checked&&!u.checked&&t.value?t.value:null,arrival_unknown:c.checked&&u.checked,is_late:c.checked&&mins>0,late_reason:c.checked&&mins>0&&lr?.value.trim()?lr.value.trim():null,entry_mode:live?'live':'after_meeting'};const {error}=await sb.from('attendance').upsert(payload,{onConflict:'meeting_id,member_id'});if(error)throw error}alert('Présences enregistrées.');closeModal();await loadData();render()}
const _openMeetingRules=openMeeting;openMeeting=function(id){const m=db.meetings.find(x=>x.id===id);if(!m)return;const phase=meetingPhase(m),live=attendanceWindow(m),past=phase==='past';modal.innerHTML=`<div class="modal-card"><button class="close" onclick="closeModal()">×</button><h2>${esc(m.title)}</h2><p>${m.scheduled_date} · ${familyName(m.family_id)}</p><div class="row-actions">${live||past?`<button class="primary" onclick="openAttendance('meeting',${id})">✓ ${live?'Présences en cours':'Présences a posteriori'}</button>`:''}${past&&m.reporting_required!==false?`<button class="primary" onclick="openReporting(${id})">Reporting</button>`:''}<button class="ghost" onclick="scheduleChange('meeting',${id},'postponed')">Reporter</button><button class="ghost" onclick="scheduleChange('meeting',${id},'cancelled')">Annuler</button></div>${!live&&!past?'<p class="muted">Les présences ouvriront 20 minutes avant le début.</p>':''}</div>`;modal.classList.remove('hidden')};